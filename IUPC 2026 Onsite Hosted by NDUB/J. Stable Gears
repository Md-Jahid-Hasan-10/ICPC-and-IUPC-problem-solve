#include <bits/stdc++.h>
using namespace std;
#define int long long

signed main(){
    int t = 1;
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        cout << ((n + 1) / 2) * (n / 2) << endl;
    }
    return 0;
}
